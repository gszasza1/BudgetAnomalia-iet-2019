language: java
jdk:
  - openjdk8
before_install: 
  - sudo apt-get install ant
script:
  - ant clean dist junit jacoco
  - mvn test
deploy:
  provider: script
  script: netlify deploy -s $NETLIFY_SITE_ID -t $NETLIFY_ACCESS_TOKEN -p --dir ./report
  skip_cleanup: true